package com.shopsphere.gateway_server.config;

@SuppressWarnings("all")
public final class NoopRateLimiterConfig {
    /**
     * This config class is intentionally empty. It's required only to satisfy Spring Cloud Gateway's
     * RateLimiter interface contract. All configuration is handled internally.
     */
}
